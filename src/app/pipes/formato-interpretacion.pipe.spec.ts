import { FormatoInterpretacionPipe } from './formato-interpretacion.pipe';

describe('FormatoInterpretacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoInterpretacionPipe();
    expect(pipe).toBeTruthy();
  });
});
